# Configuraciones globales

# Dirección IP y puerto donde escuchará el proxy
PROXY_HOST = '0.0.0.0'
PROXY_PORT = 8888

# Dominios que bloquearemos
BLACKLIST = [
    "facebook.com",
    "twitter.com",
    "instagram.com"
]

# Tamaño del buffer de lectura (bytes)
BUFFER_SIZE = 4096
